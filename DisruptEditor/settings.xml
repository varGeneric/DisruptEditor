<Settings>
	<PackFolder src="PATH TO STOCK UNPACKS\patch_unpack" />
	<PackFolder src="PATH TO STOCK UNPACKS\worlds\windy_city\windy_city_unpack" />
	<PackFolder src="PATH TO STOCK UNPACKS\common_unpack" />

	<patchDir src="PATH TO MODDED PATCH\patch_unpack" />
	<textDrawDistance src="5" />
	<drawBuildings src="0" />

	<Controls>
		<!-- Use the decimal values from https://wiki.libsdl.org/SDLScancodeLookup -->
		<Forward  key="26" />
		<Backward key="22" />
		<Left     key="4" />
		<Right    key="7" />
		<Ascend   key="44" />
		<Descend  key="226" />

		<!--
			Note: These are hex values, the setting uses decimal
			KMOD_NONE = 0x0000,
			KMOD_LSHIFT = 0x0001,
			KMOD_RSHIFT = 0x0002,
			KMOD_LCTRL = 0x0040,
			KMOD_RCTRL = 0x0080,
			KMOD_LALT = 0x0100,
			KMOD_RALT = 0x0200,
			KMOD_LGUI = 0x0400,
			KMOD_RGUI = 0x0800,
			KMOD_NUM = 0x1000,
			KMOD_CAPS = 0x2000,
			KMOD_MODE = 0x4000,
			KMOD_RESERVED = 0x8000 

			These can be logically OR'd together to require multiple
		-->
		<Fast keyMod="1" />
		<Slow keyMod="64" />
	</Controls>
</Settings>